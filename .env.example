TMDB_API_KEY=
# Use the provider's pooled/serverless connection for the deployed app.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# Optional: use a direct connection when running database migrations locally.
DATABASE_MIGRATION_URL=
ADMIN_PASSCODE=
